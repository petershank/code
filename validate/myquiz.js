var assert = require("assert");
// const validate = require("../validate");

describe("the universe", function () {
  it("knows 1 = 1", function (done) {
    assert.strictEqual(1, 1);
    done();
  });
});
