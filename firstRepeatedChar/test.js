"use strict";

const assert = require("assert");
// const myFunc = require('index')

describe("the unit under an easy test", function () {
  it("passes an easy test", function () {
    assert.strictEqual(1, 45);
  });
});
